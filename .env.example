# Optional: used by docker compose for broker metadata (Kafka clients reconnect using this host).
# - Local dev on one machine only: leave as localhost or omit file.
# - Remote clients need the VM hostname or reachable IP Kafka returns in metadata:
#   KAFKA_ADVERTISED_PUBLIC_HOST=my-server.example.com
KAFKA_ADVERTISED_PUBLIC_HOST=localhost
